# Compressed model artifact

The latest compressed DeepMD frozen graph is `compressed.pb` at this repository's root.  It is an intentionally versioned release artifact and is distinct from the baseline `model/graph.pb`.

Before deployment, validate it with a DeepMD-kit version compatible with the graph and record the command and `dp test` result in the release or pull request.
