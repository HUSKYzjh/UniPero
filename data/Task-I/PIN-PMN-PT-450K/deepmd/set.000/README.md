# PIN-PMN-PT-450K NumPy set

This directory is DeepMD-kit set `000` for PIN-PMN-PT at 450K.  It contains `box.npy`, `coord.npy`, `energy.npy`, `force.npy`, and `virial.npy` with a common frame axis.  The atom species order is defined by the parent `type.raw` and `type_map.raw` files.  Treat these arrays as generated data: regenerate them from a validated raw-data set instead of editing individual arrays.
